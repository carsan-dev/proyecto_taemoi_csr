Comandos arrancar server

  # 3. Stop the containers
  sudo docker-compose -f docker-compose.production.yml down

  # 4. Rebuild the backend with the fix (no cache to ensere fresh build)
  sudo docker-compose -f docker-compose.production.yml build --no-cache backend

  # 5. Start all containers
  sudo docker-compose -f docker-compose.production.yml up -d

  # 6. Monitor the backend logs to ensure it starts successfully
  sudo docker-compose -f docker-compose.production.yml logs -f backend

  # 7. Eliminate the old images of containers
  sudo docker system prune -af

Migrar data para local: 

python migrate_to_mysql_v7.py --access "C:\Users\croly\Documents\proyecto_taemoi_csr\CLUBMOISKIMDO_AUX.mdb" --out "C:\Users\croly\Documents\proyecto_taemoi_csr\migration_local.sql" --env local --schema taemoidb

Migrar data para server: 

python migrate_to_mysql_v7.py --access "C:\Users\croly\Documents\proyecto_taemoi_csr\CLUBMOISKIMDO_AUX.mdb" --out "C:\Users\croly\Documents\proyecto_taemoi_csr\migration_server.sql" --env server --schema taemoi_db

Migrar data para docker: 

python migrate_to_mysql_v7.py --access "C:\Users\croly\Documents\proyecto_taemoi_csr\CLUBMOISKIMDO_AUX.mdb" --out "C:\Users\croly\Documents\proyecto_taemoi_csr\migration_docker.sql" --env docker --schema taemoi_db

TODO:

the mensualidad product have to have diferent prices depending on the tipo_tarifa in the alumn. the adults 30, non adults 28, hermanos 26, padre hijo the father 28 and the child 26 (i have to be capable of asociating a father with his child with some logic)

Change the taekwondo asociated tipo tarifa for removing PILATES and DEFENSA_PERSONAL_FEMENINA, the kickboxing asociated to create tipo tarifa for especifically kickboxing with the actual prices in the asociated kickboxing tipo tarifa and the Pilates and Defensa_Personal_Femenina with the tipo_tarifa with his names

if the age introduced in the crear alumno is infantil (follow the logic in the app to know what alumns are kids or adults. Search for menor de 13 or similar) the grados are showing in the grados selector are all, but if adult, the grados are only the complete grados. If i change the age, change the showing grades respectively

change all the filters for search by name to follow the logic in the filter in listado-alumnos for wait a little to search

repair the pagination or redo for correct functioning, because when i create a new alumn, the pagination cant showing it in the editar-alumno page.

remove the closeAdminMenu console log

add a filter for searching by name in grupo-alumnos modal

when clicking crear convocatoria, doesnt appears any swall message and i want a confirmation with the following message (Vas a crear una convocatoria de "Deporte" para "fechaConvocatoria". Estás seguro?) and the option to change the sport. Create to a input for selecting the fechaConvocatoria 

fix the listado-convocatorias component to show the convocatoria details more accesible and atractive and modern