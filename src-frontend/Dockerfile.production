# Multi-stage build for production
FROM node:22-alpine AS build

WORKDIR /app

# Copy package files
COPY package*.json ./

# Install ALL dependencies (including dev dependencies needed for build)
RUN npm ci && npm cache clean --force

# Copy source code
COPY . .

# Build for production
ARG ENVIRONMENT=production
RUN npm run build:production

# Production stage with Alpine nginx + Brotli support
FROM fholzer/nginx-brotli:v1.26.2

# Install curl for healthcheck
RUN apk add --no-cache curl

# Remove default nginx config
RUN rm /etc/nginx/nginx.conf
RUN rm /etc/nginx/conf.d/default.conf

# Copy built Angular app
COPY --from=build /app/dist/taemoi-angular-front/browser /usr/share/nginx/html

# Copy nginx configuration (will be overridden by volume mount in docker-compose)
COPY nginx.conf /etc/nginx/nginx.conf

# Reload nginx when Certbot replaces the mounted certificate files.
COPY nginx-cert-reloader.sh /usr/local/bin/nginx-cert-reloader
RUN chmod +x /usr/local/bin/nginx-cert-reloader

# Create directories for Let's Encrypt
RUN mkdir -p /var/www/certbot

# Set proper permissions
RUN chown -R nginx:nginx /usr/share/nginx/html && \
    chown -R nginx:nginx /var/cache/nginx && \
    chown -R nginx:nginx /var/log/nginx && \
    chmod -R 755 /usr/share/nginx/html

# Expose ports
EXPOSE 80 443

# Health check
HEALTHCHECK --interval=30s --timeout=10s --start-period=5s --retries=3 \
  CMD curl -f http://localhost/health || exit 1

# Start nginx
ENTRYPOINT ["/usr/local/bin/nginx-cert-reloader"]
CMD ["-g", "daemon off;"]
