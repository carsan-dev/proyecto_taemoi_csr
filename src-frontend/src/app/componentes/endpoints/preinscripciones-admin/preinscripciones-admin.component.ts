import { Component,OnInit } from '@angular/core'; import { CommonModule } from '@angular/common'; import { FormsModule } from '@angular/forms'; import { PreinscripcionService } from '../../../features/preinscripcion/preinscripcion.service';
@Component({selector:'app-preinscripciones-admin',standalone:true,imports:[CommonModule,FormsModule],templateUrl:'./preinscripciones-admin.component.html',styleUrl:'./preinscripciones-admin.component.scss'})
export class PreinscripcionesAdminComponent implements OnInit {items:any[]=[];cargando=false;filtros={temporada:'',deporte:'',estado:'',q:''};seleccion:any;alumnoId?:number;mensaje='';constructor(private readonly api:PreinscripcionService){}ngOnInit(){this.buscar();}buscar(){this.cargando=true;this.api.listar(this.filtros).subscribe({next:x=>{this.items=x;this.cargando=false},error:()=>this.cargando=false});}seleccionar(x:any){this.seleccion=x;this.mensaje='';}cancelar(){if(!this.seleccion||!confirm('¿Cancelar esta solicitud?'))return;this.api.cancelar(this.seleccion.referencia).subscribe(()=>{this.seleccion=undefined;this.buscar();});}reenviar(){if(!this.seleccion)return;this.api.reenviar(this.seleccion.referencia).subscribe(()=>this.mensaje='Correo reenviado.');}finalizar(){if(!this.seleccion||!this.alumnoId)return;this.api.finalizar(this.seleccion.referencia,this.alumnoId).subscribe({next:()=>{this.seleccion=undefined;this.buscar();},error:e=>this.mensaje=e?.error?.mensaje||'No se pudo finalizar.'});}}
